import { FilterOrderPipe } from './filter-order.pipe';

describe('FilterOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
