import { FilterRestaurantPipe } from './filter-restaurant.pipe';

describe('FilterRestaurantPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRestaurantPipe();
    expect(pipe).toBeTruthy();
  });
});
